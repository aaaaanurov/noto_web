# 🚀 Quick Start - Что делать в Vercel

## ✅ Шаг 1: Добавить Environment Variables (ОБЯЗАТЕЛЬНО!)

Перейдите в Vercel Dashboard → ваш проект → **Settings** → **Environment Variables**

Добавьте 3 переменные (для всех окружений: Production, Preview, Development):

### 1. NEXT_PUBLIC_SUPABASE_URL
```
https://ваш-проект.supabase.co
```
Где взять: **Supabase Dashboard** → Settings → API → Project URL

### 2. NEXT_PUBLIC_SUPABASE_ANON_KEY
```
ваш_anon_key
```
Где взять: **Supabase Dashboard** → Settings → API → anon public (скопируйте)

### 3. NEXT_PUBLIC_APP_URL
```
https://noto.space
```
Или пока что `https://your-project.vercel.app` если домена еще нет

---

## ✅ Шаг 2: Redeploy

После добавления переменных:

1. **Deployments** → последний деплой → три точки (⋯) → **Redeploy**
2. Подождите ~1-2 минуты
3. Проверьте что билд успешный (зеленая галочка ✅)

---

## ✅ Шаг 3: Проверить что работает

Откройте ваш Vercel URL (например, `https://noto-web.vercel.app`):

- Главная страница должна открыться ✅
- Попробуйте `/profile/testuser` (покажет 404 если профиля нет - это нормально)
- Попробуйте `/wishlist/abc123` (тоже 404 - нормально)

---

## 🌐 Шаг 4: Домен (опционально, но важно для Universal Links)

Если у вас есть домен `noto.space`:

1. Vercel → **Settings** → **Domains** → **Add Domain**
2. Введите `noto.space`
3. Vercel покажет DNS записи
4. Добавьте их в вашем регистраторе домена (Namecheap, Cloudflare и т.д.)
5. Подождите 5-30 минут для propagation

### Если домена еще нет:

Можно купить на:
- Namecheap (~$10/год)
- Cloudflare (~$10/год)
- Google Domains (~$12/год)

**Важно:** Без домена Universal Links работать не будут! iOS приложение не сможет автоматически открываться.

---

## 📱 Шаг 5: Обновить AASA файл

После того как домен заработает:

1. Откройте Xcode → Проект Noto → **Signing & Capabilities**
2. Найдите **Team ID** (например, `AB1CD2EF34`)
3. Найдите **Bundle Identifier** (например, `com.noto.app`)

4. Откройте файл:
   ```
   /Users/antonyanurov/Documents/noto-web/public/.well-known/apple-app-site-association
   ```

5. Замените `YOUR_TEAM_ID.com.noto.app` на ваш App ID, например:
   ```json
   "appID": "AB1CD2EF34.com.noto.app"
   ```

6. Закоммитьте и запушьте:
   ```bash
   cd /Users/antonyanurov/Documents/noto-web
   git add .
   git commit -m "Update AASA with real App ID"
   git push
   ```

Vercel автоматически передеплоит проект.

---

## 🧪 Тестирование

### Проверить AASA файл:
```bash
curl https://noto.space/.well-known/apple-app-site-association
```

Должен вернуть JSON без редиректов.

### Проверить OG метаданные:

Откройте https://www.opengraph.xyz/ и вставьте:
```
https://noto.space/profile/ваш_username
```

Должны увидеть красивую превью-карточку.

---

## ✅ Чеклист готовности

- [ ] Environment Variables добавлены в Vercel
- [ ] Проект успешно задеплоен (зеленая галочка)
- [ ] Главная страница открывается
- [ ] Домен настроен (опционально)
- [ ] AASA файл обновлен с реальным App ID
- [ ] iOS приложение настроено (Associated Domains)
- [ ] Universal Links протестированы на устройстве

---

## 🎉 Готово!

Теперь у вас работает:

- ✅ Web frontend
- ✅ OG метаданные для мессенджеров
- ✅ AASA для Universal Links
- ✅ Автодеплой из GitHub

**Следующий шаг:** Настройте Associated Domains в iOS приложении (см. `VERCEL_SETUP.md` → Шаг 8)

